const IMAGE_FIELD = 'image';

const PROMPT_FIELD = 'prompt';

const UPLOAD_IMAGE_TOOLTIP = "The image to edit. Must be a valid PNG file, less than 4MB, and square. If mask is not provided, image must have transparency, which will be used as the mask"

export {
  IMAGE_FIELD,
  PROMPT_FIELD,
  UPLOAD_IMAGE_TOOLTIP
}